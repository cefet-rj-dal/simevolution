<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        Customize your theme here.
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>-->

    <style name="SimEvolutionTheme" parent="@style/Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:editTextStyle">@style/SimEvolutionEditText</item>
        <item name="editTextStyle">@style/SimEvolutionEditText</item>
    </style>

    <style name="SimEvolutionEditText" parent="Widget.AppCompat.EditText">
        <item name="background">@android:color/transparent</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textCursorDrawable">@null</item>
    </style>

</resources>
